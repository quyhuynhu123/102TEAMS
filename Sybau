local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local player = Players.LocalPlayer

--[[ 
💥 FULL SCRIPT: Anti-Annoying GUI Cleaner
📌 Tự động xóa GUI độc hại trong Grow a Garden hoặc bất kỳ game nào
👁‍🗨 Tìm và loại bỏ các GUI chứa: "Script Loading Please Wait!", "discord.gg", "loading"
]]

-- Bắt đầu xóa GUI chứa nội dung không mong muốn
local unwantedKeywords = {
    "script loading please wait",
    "discord.gg",
    "loading"
}

-- Hàm kiểm tra và xóa GUI độc hại
local function removeUnwantedGUI()
    for _, gui in ipairs(game:GetDescendants()) do
        if gui:IsA("TextLabel") or gui:IsA("TextButton") then
            local lowerText = string.lower(gui.Text)
            for _, keyword in ipairs(unwantedKeywords) do
                if string.find(lowerText, keyword) then
                    local parentGui = gui:FindFirstAncestorWhichIsA("ScreenGui")
                    if parentGui and parentGui.Parent == game.CoreGui or parentGui.Parent == game.Players.LocalPlayer:FindFirstChild("PlayerGui") then
                        print("🧹 Removing ScreenGui:", parentGui.Name)
                        parentGui:Destroy()
                    else
                        print("🧹 Removing GUI Element:", gui.Name)
                        gui:Destroy()
                    end
                    break
                end
            end
        end
    end
end

-- Tự động chạy và lặp lại
task.spawn(function()
    while true do
        pcall(removeUnwantedGUI)
        task.wait(1) -- Lặp lại mỗi giây
    end
end)

-- Dữ liệu pet theo từng loại trứng
local eggPetData = {
    ["Common Egg"] = {
        {Name = "Dog", Weight = 3},
        {Name = "Golden Lad", Weight = 2},
        {Name = "Bunny", Weight = 3},
    },
    ["Uncommon Egg"] = {
        {Name = "Deer", Weight = 3},
        {Name = "Cat", Weight = 3},
        {Name = "Chicken", Weight = 3},
    },
    ["Rare Egg"] = {
        {Name = "Cow", Weight = 3},
        {Name = "Sheep", Weight = 3},
        {Name = "Goat", Weight = 3},
    },
    ["Bug Egg"] = {
        {Name = "Snail", Weight = 3},
        {Name = "Bee", Weight = 3},
        {Name = "Mantis", Weight = 3},
        {Name = "Giant Ant", Weight = 3},
        {Name = "Dragonfly", Weight = 1},
    },
    ["Zen Egg"] = {
        {Name = "Shiba Inu", Weight = 3},
        {Name = "Kappa", Weight = 3},
        {Name = "Kitsune", Weight = 1},
        {Name = "Tanuki", Weight = 3},
        {Name = "Nihonzaru", Weight = 3},
    },
    ["Mythical Egg"] = {
        {Name = "Red Fox", Weight = 1},
        {Name = "Grey Mouse", Weight = 3},
        {Name = "Squirrel", Weight = 3},
        {Name = "Tiger", Weight = 3},
        {Name = "Panda", Weight = 3},
    },
    ["Legendary Egg"] = {
        {Name = "Polar Bear", Weight = 1},
        {Name = "Peacock", Weight = 3},
        {Name = "Turtle", Weight = 3},
        {Name = "Elephant", Weight = 3},
        {Name = "Leopard", Weight = 3},
    },
    ["Dinosaur Egg"] = {
        {Name = "T-Rex", Weight = 1},
        {Name = "Stegosaurus", Weight = 3},
        {Name = "Triceratops", Weight = 3},
        {Name = "Pterodactyl", Weight = 3},
        {Name = "Velociraptor", Weight = 3},
    },
    ["Rainbow Egg"] = {
        {Name = "Unicorn", Weight = 2},
        {Name = "Rainbow Cat", Weight = 2},
        {Name = "Pegasus", Weight = 2},
        {Name = "Glitter Deer", Weight = 2},
    },
}

-- Hàm chọn pet ngẫu nhiên theo tỉ lệ
local function getRandomPet(pets)
    local totalWeight = 0
    for _, pet in ipairs(pets) do
        totalWeight += pet.Weight
    end
    local rand = math.random() * totalWeight
    local sum = 0
    for _, pet in ipairs(pets) do
        sum += pet.Weight
        if rand <= sum then
            return pet.Name
        end
    end
    return pets[1].Name
end

-- GUI
local gui = Instance.new("ScreenGui", player:WaitForChild("PlayerGui"))
gui.Name = "PetRandomizerGUI"
gui.ResetOnSpawn = false

local main = Instance.new("Frame", gui)
main.Size = UDim2.new(0, 200, 0, 60)
main.Position = UDim2.new(0.5, -100, 0, 100)
main.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
main.BackgroundTransparency = 0.3
main.Active = true
main.Draggable = true

local toggle = Instance.new("TextButton", main)
toggle.Size = UDim2.new(1, 0, 0.5, 0)
toggle.Position = UDim2.new(0, 0, 0, 0)
toggle.Text = "Toggle Randomizer"
toggle.TextColor3 = Color3.new(1, 1, 1)
toggle.BackgroundTransparency = 1
toggle.Font = Enum.Font.GothamBold
toggle.TextScaled = true

local timerLabel = Instance.new("TextLabel", main)
timerLabel.Size = UDim2.new(1, 0, 0.5, 0)
timerLabel.Position = UDim2.new(0, 0, 0.5, 0)
timerLabel.BackgroundTransparency = 1
timerLabel.TextColor3 = Color3.new(1, 1, 1)
timerLabel.Font = Enum.Font.Gotham
timerLabel.TextScaled = true
timerLabel.Text = "Next random in: 30s"

local enabled = true
local countdown = 30

toggle.MouseButton1Click:Connect(function()
    enabled = not enabled
    toggle.Text = enabled and "Randomizer ON" or "Randomizer OFF"
end)

-- Đếm ngược
task.spawn(function()
    while true do
        wait(1)
        if enabled then
            countdown -= 1
            if countdown <= 0 then
                countdown = 30
            end
        end
        timerLabel.Text = "Next random in: " .. countdown .. "s"
    end
end)

-- Theo dõi trứng và random pet
local lastRandom = {}
RunService.RenderStepped:Connect(function()
    if not enabled or countdown > 0 then return end

    for _, egg in ipairs(workspace:GetDescendants()) do
        if egg:IsA("Model") and egg:FindFirstChild("EggType") and (egg.PrimaryPart or egg:FindFirstChild("HumanoidRootPart")) then
            local root = egg.PrimaryPart or egg:FindFirstChild("HumanoidRootPart")
            local dist = (player.Character and player.Character:FindFirstChild("HumanoidRootPart") and (player.Character.HumanoidRootPart.Position - root.Position).Magnitude) or 0

            if dist <= 50 and (not lastRandom[egg] or tick() - lastRandom[egg] >= 30) then
                local eggType = egg.EggType.Value
                local petList = eggPetData[eggType]
                if petList then
                    local chosenPet = getRandomPet(petList)

                    if egg:FindFirstChild("PetLabel") then
                        egg.PetLabel:Destroy()
                    end

                    local label = Instance.new("BillboardGui", egg)
                    label.Name = "PetLabel"
                    label.Size = UDim2.new(0, 200, 0, 50)
                    label.Adornee = root
                    label.StudsOffset = Vector3.new(0, 3, 0)
                    label.AlwaysOnTop = true

                    local text = Instance.new("TextLabel", label)
                    text.Size = UDim2.new(1, 0, 1, 0)
                    text.BackgroundTransparency = 1
                    text.Text = chosenPet
                    text.TextScaled = true
                    text.Font = Enum.Font.FredokaOne
                    text.TextColor3 = Color3.new(1, 1, 0)

                    lastRandom[egg] = tick()
                end
            end
        end
    end

    countdown = 30
end)
